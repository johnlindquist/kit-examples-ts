/*
# Built-in Terminal Example

- Opens a terminal and runs the command `cd ~/Downloads && ls`
*/

// Name: Built-in Terminal Example
// Description: Open a terminal and run a command

import "@johnlindquist/kit"

await term("cd ~/Downloads && ls")